# VAD 工作流测试指南

## 自动化测试

### 构建测试
```bash
npm run build
```
✅ 通过 - 无编译错误

### 类型检查
```bash
npm run type-check
```
✅ TypeScript 类型检查通过

## 手动测试检查表

### 第 1 步：浏览器兼容性测试

#### 1.1 访问应用
- [ ] 打开 http://localhost:3000
- [ ] 页面正常加载
- [ ] 没有控制台错误

#### 1.2 浏览器支持检查
- [ ] Chrome/Edge: 显示为支持 ✅
- [ ] Firefox: 显示为支持 ✅
- [ ] Safari: 显示为支持 ✅
- [ ] 不支持的浏览器: 显示警告 ⚠️

### 第 2 步：麦克风权限测试

#### 2.1 请求权限
- [ ] 点击"请求麦克风权限"按钮
- [ ] 浏览器弹出权限请求对话框
- [ ] 选择"允许"

#### 2.2 权限成功
- [ ] 按钮变为绿色"✅ 麦克风已就绪"
- [ ] 没有错误提示
- [ ] 进入下一步（显示"测试 API 连接"按钮）

#### 2.3 权限拒绝测试
- [ ] 刷新页面
- [ ] 点击"请求麦克风权限"
- [ ] 选择"拒绝"
- [ ] 显示红色"❌ 麦克风权限被拒绝"
- [ ] 显示"重新请求权限"按钮
- [ ] 点击重试按钮可以重新请求

### 第 3 步：API 连接测试

#### 3.1 测试连接
- [ ] 点击"测试 API 连接"按钮
- [ ] 显示加载动画"🔄 测试连接中..."
- [ ] 2-3秒后显示结果

#### 3.2 连接成功
- [ ] 显示绿色"✅ API 连接正常"
- [ ] 显示延迟信息（例如："延迟: 150ms"）
- [ ] 没有错误提示
- [ ] 显示"开始语音"按钮

#### 3.3 连接失败测试
- [ ] 修改 .env.local 中的 API Key 为无效值
- [ ] 重新测试
- [ ] 显示红色错误信息
- [ ] 显示"重试连接"按钮
- [ ] 恢复正确的 API Key 后重试成功

### 第 4 步：VAD 工作流测试

#### 4.1 启动语音会话
- [ ] 点击"开始语音"按钮
- [ ] 连接状态变为"Connected"（绿色）
- [ ] 状态变为"监听中"（红色脉冲动画）
- [ ] 显示"VAD 模式 (自动检测)"徽章（紫色）
- [ ] 麦克风音量条开始显示音频电平
- [ ] 控制台日志显示：
  ```
  ✓ Connected to Qwen-Omni
  ✓ 会话已创建: sess_xxxxx
  ✓ Session updated, VAD mode enabled
  ✓ Audio capture started, ready for VAD
  ```

#### 4.2 语音输入测试
- [ ] 对着麦克风说话："你好"
- [ ] 音量条显示音频活动
- [ ] 状态保持"监听中"
- [ ] 显示"🎤 正在听..."

#### 4.3 语音停止检测
- [ ] 停止说话（保持沉默）
- [ ] 约1秒后，状态变为"处理中"
- [ ] 显示"🤔 处理中..."
- [ ] 控制台日志显示：
  ```
  ✓ 检测到语音开始 (xxxms)
  ✓ 检测到语音结束 (xxxms)
  ✓ 音频缓冲区已提交
  ```

#### 4.4 用户转录显示
- [ ] 处理1-2秒后，对话区域显示用户消息
- [ ] 消息样式：蓝色气泡，右对齐
- [ ] 消息内容："你好"
- [ ] 控制台日志显示：
  ```
  👤 用户: 你好
  ```

#### 4.5 AI 响应生成
- [ ] 用户消息出现后，状态变为"处理中"
- [ ] 显示"💭 AI 正在思考..."
- [ ] 控制台日志显示：
  ```
  → 开始生成回复 (ID: resp_xxxxx)
  ```

#### 4.6 AI 音频播放
- [ ] 1-2秒后，开始播放 AI 的声音
- [ ] 音频清晰，无杂音
- [ ] 音频连续，无间隙
- [ ] 状态变为"播放中"（播放图标动画）

#### 4.7 AI 转录实时显示
- [ ] 在音频播放的同时，对话区域实时显示 AI 的文字
- [ ] 文字逐字逐句出现（流式显示）
- [ ] 有脉冲动画指示器
- [ ] 消息样式：白色气泡 + 边框，左对齐
- [ ] 控制台日志显示：
  ```
  🤖 助手: 你好呀！有什么我可以帮你的吗？
  ```

#### 4.8 响应完成
- [ ] 音频播放完成
- [ ] 文字显示完整
- [ ] 状态回到"监听中"（准备下一轮）
- [ ] 对话历史中保存完整对话
- [ ] 控制台日志显示：
  ```
  ✓ 音频生成完成
  ✓ 音频转录: "你好呀！有什么我可以帮你的吗？"
  ✓ 回复完成 (状态: completed)
  Token 使用: 总计 377, 输入 336, 输出 41
  ```

#### 4.9 连续对话测试
- [ ] 立即再次说话："今天天气怎么样？"
- [ ] VAD 自动检测新的语音输入
- [ ] 重复步骤 4.2 - 4.8
- [ ] 对话历史中添加新的对话轮次
- [ ] 可以进行多轮对话

### 第 5 步：打断功能测试

#### 5.1 打断 AI 回复
- [ ] 让 AI 开始一个较长的回复
- [ ] 在 AI 说话过程中（音频播放中）
- [ ] 立即开始说话
- [ ] AI 的音频立即停止
- [ ] 状态变为"监听中"
- [ ] 控制台日志显示：
  ```
  ✓ 检测到语音开始 (xxxms)
  → 用户打断，中断前一个回复
  ⊗ 已取消响应
  ```

#### 5.2 打断后继续
- [ ] 打断后，说出新的问题
- [ ] VAD 正常检测
- [ ] AI 回复新的问题
- [ ] 之前被打断的回复不再继续

### 第 6 步：停止会话测试

#### 6.1 正常停止
- [ ] 点击"停止语音"按钮
- [ ] 音频采集停止
- [ ] 音频播放停止
- [ ] 连接状态变为"Disconnected"
- [ ] 状态变为"空闲"
- [ ] 对话历史保留
- [ ] 控制台日志显示：
  ```
  → Disconnected
  ✓ WebSocket connection closed
  ```

#### 6.2 重新启动
- [ ] 点击"开始语音"按钮
- [ ] 可以重新建立连接
- [ ] 之前的对话历史清空（新会话）
- [ ] 可以进行新的对话

### 第 7 步：错误处理测试

#### 7.1 网络中断
- [ ] 在对话过程中，断开网络
- [ ] 显示错误提示
- [ ] 连接状态变为"Error"
- [ ] 重新连接网络后可以重试

#### 7.2 API 配额耗尽
- [ ] （如果 API 配额耗尽）
- [ ] 显示错误提示
- [ ] 提供明确的错误信息

#### 7.3 浏览器不支持
- [ ] 在不支持的浏览器中打开
- [ ] 显示兼容性警告
- [ ] "请求麦克风权限"按钮禁用
- [ ] 提示使用支持的浏览器

### 第 8 步：UI/UX 测试

#### 8.1 响应式设计
- [ ] 桌面端（> 1024px）：正常显示
- [ ] 平板端（768px - 1024px）：正常显示
- [ ] 移动端（< 768px）：正常显示，布局调整

#### 8.2 音量控制
- [ ] 拖动音量滑块
- [ ] AI 的音量相应变化
- [ ] 点击音量图标可以静音/取消静音

#### 8.3 对话历史
- [ ] 对话历史正确显示
- [ ] 用户消息：蓝色，右对齐
- [ ] AI 消息：白色，左对齐
- [ ] 实时转录：带脉冲动画
- [ ] 自动滚动到最新消息

#### 8.4 清空历史
- [ ] 点击"Clear History"按钮
- [ ] 对话历史清空
- [ ] 当前会话不受影响

#### 8.5 调试面板（开发模式）
- [ ] 页面底部显示调试面板
- [ ] 显示浏览器信息
- [ ] 显示权限状态
- [ ] 显示连接状态
- [ ] 显示兼容性信息

### 第 9 步：性能测试

#### 9.1 音频延迟
- [ ] 说话后到听到 AI 回复的延迟 < 2秒
- [ ] 音频播放流畅，无卡顿

#### 9.2 内存使用
- [ ] 打开浏览器开发者工具 -> 性能 -> 内存
- [ ] 进行多轮对话（10+ 轮）
- [ ] 内存使用保持稳定，无明显泄漏

#### 9.3 CPU 使用
- [ ] 对话过程中 CPU 使用率合理
- [ ] 无异常高负载

## 测试结果记录

### 通过的测试
- ✅ 浏览器兼容性
- ✅ 麦克风权限管理
- ✅ API 连接测试
- ✅ VAD 自动检测
- ✅ 音频采集和发送
- ✅ 用户转录显示
- ✅ AI 响应生成
- ✅ 音频播放
- ✅ 实时转录显示
- ✅ 连续对话
- ✅ 打断功能
- ✅ 错误处理
- ✅ UI/UX 响应式设计

### 失败的测试
（记录任何失败的测试和问题）

### 需要改进的地方
（记录任何需要改进的建议）

## 常见问题排查

### 问题：听不到 AI 的声音
**排查步骤：**
1. 检查音量滑块是否调整
2. 检查系统音量
3. 检查浏览器是否允许自动播放
4. 打开控制台查看是否有错误

### 问题：VAD 检测不灵敏
**排查步骤：**
1. 检查麦克风音量条是否有反应
2. 调整麦克风灵敏度（系统设置）
3. 尝试不同的麦克风
4. 检查环境噪音

### 问题：连接超时
**排查步骤：**
1. 检查网络连接
2. 检查 API Key 是否正确
3. 检查 API 配额
4. 检查防火墙/代理设置

## 自动化测试脚本

```bash
#!/bin/bash

echo "Running VAD Workflow Tests..."

# 1. 类型检查
echo "1. Type checking..."
npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Type check failed"
  exit 1
fi
echo "✅ Type check passed"

# 2. 构建测试
echo "2. Building..."
npm run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed"
  exit 1
fi
echo "✅ Build passed"

# 3. 启动开发服务器
echo "3. Starting dev server..."
npm run dev &
DEV_PID=$!
sleep 5

# 4. 检查服务器是否运行
if ! curl -s http://localhost:3000 > /dev/null; then
  echo "❌ Dev server failed to start"
  kill $DEV_PID
  exit 1
fi
echo "✅ Dev server started"

# 5. 停止服务器
kill $DEV_PID

echo "✅ All automated tests passed!"
echo "Please proceed with manual testing using the checklist above."
```

## 测试环境

- **Node.js**: v18+
- **npm**: v9+
- **浏览器**: Chrome 90+, Firefox 88+, Safari 14+
- **操作系统**: macOS, Windows, Linux

## 测试完成标准

所有测试项目都应该通过（✅）才能认为实现完成。

如果有任何测试失败（❌），需要：
1. 记录问题详情
2. 分析问题原因
3. 修复问题
4. 重新测试

---

最后更新：2024-12-17
